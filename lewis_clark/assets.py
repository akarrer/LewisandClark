"""Runtime game data: populated by :func:`lewis_clark.config.load_all` before pygame screens import.

After loading, ``game_config`` holds the same values as a :class:`lewis_clark.game_config.GameConfig`
instance; palette colours are also copied onto this module as top-level names.
"""
